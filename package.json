{
  "name": "findit-stitch",
  "version": "1.0.0",
  "description": "FindIt multi-page UI integrated with Express and MySQL",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "db:schema": "node scripts/db-schema.js",
    "db:seed": "node scripts/db-seed.js",
    "db:init": "npm run db:schema && npm run db:seed"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-session": "^1.19.0",
    "multer": "^2.0.2",
    "mysql2": "^3.11.0"
  }
}
